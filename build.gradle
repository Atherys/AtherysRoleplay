plugins {
    id 'com.github.johnrengelman.shadow' version '2.0.2'
    id 'com.atherys.gradle' version '1.1.5'
    id 'net.kyori.blossom' version '1.1.0'
}

group 'com.atherys'

version '1.1.0'

blossom {
    replaceToken '%PROJECT_VERSION%', project.version
}